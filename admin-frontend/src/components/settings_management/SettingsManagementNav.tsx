import { Building2, Mail, MailCheck, MapPin, ShieldCheck, University, UsersRound } from 'lucide-react';import { NavLink } from 'react-router-dom';
const pages=[['/admin/settings/admins','관리자관리',ShieldCheck],['/admin/settings/member-grades','회원등급관리',UsersRound],['/admin/settings/shipping-addresses','배송지주소관리',MapPin],['/admin/settings/deposit-accounts','입금계좌관리',University],['/admin/settings/mail-address','메일주소관리',MailCheck],['/admin/settings/mail','메일관리',Mail],['/admin/settings/company-info','업체정보변경',Building2]] as const;
export function SettingsManagementNav(){return <nav className="settings-management-tabs" aria-label="환경설정 메뉴">{pages.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`settings-management-tab${isActive?' active':''}`}><Icon size={16}/><span>{label}</span></NavLink>)}</nav>}
export function SettingsPageHeader({title,description}:{title:string;description:string}){return <><div className="admin-page-header"><div><p className="admin-eyebrow">환경설정</p><h1>{title}</h1><p>{description}</p></div></div><SettingsManagementNav/></>}
